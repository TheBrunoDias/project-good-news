{
  "name": "@daily-good-news/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/handler.ts",
    "dev:local": "sst shell tsx watch src/server.ts",
    "lint": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@daily-good-news/db": "^0.0.1",
    "@fastify/aws-lambda": "^6",
    "@google/genai": "^2.7.0",
    "fastify": "^5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8",
    "@types/node": "^25",
    "tsx": "^4",
    "typescript": "^6",
    "vitest": "^4.1.8"
  }
}
